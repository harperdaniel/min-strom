services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: minstrom
      POSTGRES_PASSWORD: minstrom
      POSTGRES_USER: minstrom
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:

